using UnityEngine;

/// <summary>
/// 1. ItemData를 기반으로 아이템 클래스 생성
/// </summary>
public class Item : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
