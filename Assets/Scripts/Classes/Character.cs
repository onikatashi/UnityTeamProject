using UnityEngine;

/// <summary>
/// 1. 캐릭터 스탯 (체력, 마나, 공격력, 방어력 등)
/// 2. 캐릭터 행동 (이동, 공격, 대쉬 등)
/// 3. 캐릭터 애니메이션 제어
/// </summary>
public class Character : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
