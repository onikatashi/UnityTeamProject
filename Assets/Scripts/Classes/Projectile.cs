using UnityEngine;

/// <summary>
/// 1. 투사체 정보
/// 2. 투사체 오브젝트 풀에 반환
/// 3. 투사체 이동 (유도 투사체일 때)
/// 4. 투사체 충돌처리
/// 5. 투사체 애니메이션 제어 (충돌 시 이펙트 등)
/// 6. 투사체 충돌 시 데미지 적용
/// 7. 투사체 수명 관리 (후순위)
/// </summary>
public class Projectile : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
